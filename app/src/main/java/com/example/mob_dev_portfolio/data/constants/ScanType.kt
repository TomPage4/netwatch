package com.example.mob_dev_portfolio.data.constants

object ScanType {

    const val HOST_SCAN = "Host Scan"
    const val SERVICE_DISCOVERY = "Service Discovery"

    val allScanTypes = listOf(
        HOST_SCAN,
        SERVICE_DISCOVERY
    )
}